<head>
  <title>Habits</title>
  <style>
    body{
        font-family: sans-serif;
        background-color: #E9DDD4;
    }
    .main-Container {
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
      align-items: center;
      /* border: 1px solid; */
      height: 96vh;
    }
    .main-Container h1{
        font-family: sans-serif;
    }
    .main-Container form input{
        margin-right: -16px;
    height: 6vh;
    font-size: 20px;
    padding-left: 10px;
    border-radius: 15px;
    border: 4px solid #F79489;
    border-right: none;
    }
    .main-Container form button{
    height: 6vh;
    font-size: 20px;
    padding: 10px;
    border-radius: 15px;
    border: 4px solid #F79489;
    background-color: #F9F1F0;
    cursor: pointer;
    color: #900020;
    font-weight: 600;
    font-family: sans-serif;
    width: 22vw;

    }
    .label-select select{
      height: 4vh;
    width: 22vw;
    font-size: 20px;
    text-align: center;
    font-weight: 500;
    cursor: pointer;
    border-radius: 10px;
    }
    .main-Container li{
        text-decoration: none;
    color: #900020;
    font-family: sans-serif;
    font-size: 36px;
    font-weight: 600;
    }
    .main-Container li::marker{
        text-decoration: none;
    color: #900020;
    font-family: sans-serif;
    font-size: 36px;
    font-weight: 600;
    }
    .label-select{
      display: flex;
    flex-direction: column;
    height: 10vh;
    justify-content: space-evenly;
    font-size: 30px;
    }
  </style>
</head>

<body>
<div class="main-Container">
<h1 class="H1tag"><%= habit.name %></h1>
<form action="/habit/<%= habit._id %>/update" method="POST">
  <div class="label-select">
  <label for="status">Today's Status:</label>
  <select name="status" id="status">
    <option value="Done">Done</option>
    <option value="Not done">Not done</option>
    <option value="None">None</option>
  </select>
</div>
  <button type="submit">Update</button>
</form>
<h2>Last 7 days:</h2>
<ul>
  <% habit.history.slice(-7).forEach(entry => { %>
    <li><%= entry.date.toDateString() %>: <%= entry.status %></li>
  <% }); %>
</ul>
</div>
</body>